version: '2'
services:
    acceptance:
        image: testing
        networks:
            - mender
        volumes:
            - "${TESTS_DIR}:/tests"
            - /var/run/docker.sock:/var/run/docker.sock
